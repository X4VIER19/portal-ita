<%- include('../partials/head', { titulo: 'Nuevo usuario' }) %>
<body>
    <div class="app-shell">
        <%- include('../partials/sidebar', {
            activo: 'usuarios',
            admin: { nombre, apellido, rol }
        }) %>

        <div class="contenido-principal">
            <%- include('../partials/topbar', { tituloSeccion: 'Nuevo usuario' }) %>

            <main class="contenido-pagina">
                <a href="/admin/usuarios" class="volver">&larr; Volver a usuarios</a>

                <div class="form-panel">
                    <% if (error) { %>
                        <div class="error-box"><%= error %></div>
                    <% } %>

                    <form method="POST" action="/admin/usuarios/nuevo">
                        <%- include('../partials/form-usuario-campos', {
                            valores,
                            modo: 'crear',
                            prefijo: ''
                        }) %>
                    </form>
                </div>
            </main>
        </div>
    </div>
</body>
</html>