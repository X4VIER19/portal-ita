<%- include('../partials/head', { titulo: 'Portal ITA' }) %>
<body>
    <style>
        body {
            background: var(--color-fondo-app);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
            text-align: center;
        }
        h1 { color: var(--color-texto-titulo); margin-bottom: 0.5rem; }
        p { color: var(--color-texto-tenue); max-width: 40ch; }
    </style>

    <div>
        <h1>Portal ITA</h1>
        <p>Esta página se debe abrir a través del portal cautivo de la red WiFi.</p>
    </div>
</body>
</html>
